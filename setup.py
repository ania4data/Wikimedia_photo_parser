from setuptools import setup

setup(name='wikiphotoparser',
      version='0.1',
      description='Download photo, author, credit, links from wikimedia common by parsing pages',
      packages=['wikiphotoparser'],
      zip_safe=False)